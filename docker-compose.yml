version: "3"
services:
  web:
    # replace username/repo:tag with your name and image details
    image: label_tool
    deploy:
      replicas: 1
      resources:
      restart_policy:
        condition: on-failure
    ports:
      - "11000:80"
    networks:
      - webnet
networks:
  webnet: